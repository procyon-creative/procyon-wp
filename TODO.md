# TODO

## Future Improvements
- [ ] Publish as an npm module
- [ ] Add tests for command handlers (currently only testing library code)
- [ ] `plugin.js` uses synchronous `execSync` — could be async
- [ ] `csv-parser` dependency only used by `plugin.js` — consider if this command is still needed
